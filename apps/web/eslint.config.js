import baseConfig from "@repo/eslint-config/base";
import nextConfig from "@repo/eslint-config/next";

export default [
  baseConfig,
  nextConfig,
  {
    //app related custom rules can go here
  },
];
